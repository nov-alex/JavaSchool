{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Ack Data",
  "description": "AckData for ack operation",
  "type": "object",
  "properties": {
    "start": {
      "description": "Start Date for ack",
      "type": "array",
      "format": "date"
    },
    "end": {
      "description": "End Date for ack",
      "type": "array",
      "format": "date"
    },
    "hash": {
      "description": "Hash data of transaction",
      "type": "string"
    }
  },
  "required": ["start", "end", "hash"]
}